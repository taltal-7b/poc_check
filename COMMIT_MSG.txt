Fix permissions data format handling

Issues fixed:
- JSON.parse error when permissions is already array format
- Mixed data format (string vs array) causing crashes
- Type mismatch between backend and frontend

Changes:
Backend:
- Normalize permissions to array format in all endpoints
- Ensure consistent data format in responses

Frontend:
- Add parsePermissions helper for safe parsing
- Handle both string (legacy) and array (new) formats
- Update Role type to accept string | string[]
- Use parsePermissions in all permission handling code
