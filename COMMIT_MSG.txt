Fix role edit functionality

Issues fixed:
- useCreateRole and useUpdateRole were converting permissions array to comma-separated string
- Backend expects array format, not string
- Improved role type detection logic for exact match comparison

Changes:
- Remove permissions.join(',') conversion in hooks
- Send permissions as array directly to backend
- Update role type detection to compare exact permission sets
